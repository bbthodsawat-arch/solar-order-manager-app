rules_version = '2';

service cloud.firestore {
  match /databases/{database}/documents {
    // Temporary baseline: require Firebase Authentication for all application data.
    // Collection-specific authorization should be tightened in the next hardening pass.
    match /{document=**} {
      allow read, write: if request.auth != null;
    }
  }
}
